<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Queries déclarées pour détecter et ouvrir les applications de paiement installées (Wave, Orange Money, MoMo, etc.) -->
    <queries>
        <!-- Wave Mobile Money Sénégal & International -->
        <package android:name="com.wave.personal" />
        <package android:name="com.wave.business" />

        <!-- Orange Money Sénégal / Orange Money Afrique / Max it -->
        <package android:name="com.orange.sn.orangemoneysn" />
        <package android:name="com.orange.orangemoneyafrique" />
        <package android:name="com.orange.omservices" />
        <package android:name="com.orange.maxit.sn" />

        <!-- Free Money Sénégal -->
        <package android:name="sn.free.freemoney" />
        <package android:name="com.free.freemoney" />

        <!-- MTN MoMo -->
        <package android:name="com.mtn.momo" />
        <package android:name="com.momo.agent" />

        <!-- Intent schemes pour liens profonds et composeur USSD -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="wave" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service Android de simulation et diffusion des notifications SMS de livraison -->
        <service
            android:name=".service.SmsDeliveryNotificationService"
            android:exported="false" />
    </application>

</manifest>
